# Backend Environment Variables
JWT_SECRET=your-very-secret-key-change-this-in-production
PORT=3000
DB_PATH=./deepmatch.db

# LLM Configuration
# Use LM Studio (port 1234) or Ollama (port 11434)
LLM_HOST=http://localhost:1234
LLM_MODEL=local-model

# Optional: Email for magic link auth (future feature)
# EMAIL_PROVIDER=sendgrid
# SENDGRID_API_KEY=your-api-key
